@model IEnumerable<IsTakipSistemiMvc.Models.Birim>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BirimAd)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BirimAd)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BirimId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BirimId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BirimId })
        </td>
    </tr>
}

</table>
